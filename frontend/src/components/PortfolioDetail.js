import React from "react";

export const Education = ({ref}) => {
    return (
        <form ref={ref}>
            <input 
                
            />
        </form>
    );
}

export const Award = () => {
    return (
        <div>
            
        </div>
    );
}

export const Project = () => {
    return (
        <div>
            
        </div>
    );
}

export const Certificate = () => {
    return (
        <div>
            
        </div>
    );
}